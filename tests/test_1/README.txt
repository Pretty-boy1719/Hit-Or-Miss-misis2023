The test demonstrates the search for a black rectangular cross of 5*5.

Given:
image - a 16*16 image with black crosses and squares.
kernel_foreground - a black rectangular cross of 5*5.
kernel_background - not specified (set by default to a black square of 3*3).
hit_highlight - not specified (set by default to a point).

Expected result:
Points at the locations of black crosses and 5*5 black squares.

The result is in the visualization.tex

